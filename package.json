{
  "name": "@fasti/monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Fasti — an identity-first local system of record for media activity",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@11.22.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Scrobble-dev/Fasti.git"
  },
  "scripts": {
    "build": "pnpm --filter @fasti/tokens build && pnpm --filter @fasti/sdk build",
    "test": "pnpm run build && node scripts/check-js-workspace.mjs && node --test tests/js/*.test.mjs",
    "typecheck": "pnpm run build && pnpm --filter @fasti/tokens typecheck && pnpm --filter @fasti/sdk typecheck",
    "format": "prettier --write package.json pnpm-workspace.yaml tsconfig.base.json \"packages/**/*.{json,ts}\" \"scripts/**/*.mjs\" \"tests/js/**/*.mjs\"",
    "format:check": "prettier --check package.json pnpm-workspace.yaml tsconfig.base.json \"packages/**/*.{json,ts}\" \"scripts/**/*.mjs\" \"tests/js/**/*.mjs\""
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  }
}
