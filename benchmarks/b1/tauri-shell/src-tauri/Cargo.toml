[package]
name = "fasti-b1-tauri-shell"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
tauri = { version = "=2.11.5", default-features = false, features = ["wry"] }

[build-dependencies]
tauri-build = "=2.6.3"

# This benchmark fixture is deliberately isolated from Fasti's product
# workspace. It measures a future packaging primitive without creating a
# desktop product surface or an inward dependency on Tauri.
[workspace]
